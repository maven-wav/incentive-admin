# 🏷 시책 어드민 — 리뷰 수정 핸드오프 (2026-07-24, Claude Code용)

> Cowork에서 리뷰 후 나온 **수정사항 4건**을 정리한 문서다.
> 구현 + 커밋 + 배포(git push → Vercel 자동배포)는 Claude Code가 이어서 진행한다.
> **Supabase 스키마 변경(대리점 1곳 추가)이 포함**되므로 아래 SQL도 함께 적용할 것.
> 기존 `HANDOFF.md`의 용어·데이터모델·제약 설명을 먼저 숙지한 뒤 이 문서를 본다.

---

## ✅ 이미 확정된 결정 (사용자와 합의)

1. **변경 1(대리점 주체 전환)은 "전면 전환" 범위로 확정**한다.
   - 로그인/역할 스위처 주체를 **얼라이언스 3사 → VAN 대리점**으로 바꾸고,
     데이터 스코핑·권한도 **`alliance_id` → `agency_id` 기준**으로 전환한다.
   - 이는 기존 `HANDOFF.md`의 "역할 스위처에서 VAN대리점 페르소나 제거" 결정을 **의도적으로 뒤집는** 것이다.
     (완료 후 기존 HANDOFF.md의 해당 항목도 갱신할 것.)
2. **데모 대리점은 5곳만** 노출한다. 그중 **신신엠엔씨** 포함이 필수.
   - 신신엠엔씨는 시드 28곳(AG01~AG28)에 **없다** → **신규 추가(AG29)** 필요.
   - 나머지 4곳은 실제 시드 중 **가맹점 데이터가 있는 대리점**으로 선정:
     `AG01 (주)그린페이(6건)` · `AG05 (주)다온시스템(4건)` · `AG24 현페이먼트(3건)` · `AG14 (주)케이에스넷트웍스(2건)`.

---

## 변경 1 — 로그인 주체: 얼라이언스 → VAN 대리점 (얼라이언스는 등록 시 선택)

**의도**: VAN 대리점이 외부 접속해 증빙을 올리는 구조. 등록 시 "어느 얼라이언스사로 제출하는지"를 대리점이 선택한다.
현재는 반대(얼라이언스=로그인 주체, 대리점=드롭다운 선택)로 되어 있어 뒤집는다.

**영향 파일**: `lib/store.js` · `app/merchants/register/page.js` · `app/incentives/submit/page.js` · `components/Shell.js` · `app/page.js`

### 1-a. `lib/store.js`

**PERSONAS 교체** (scopeId 의미가 `alliance_id` → `agency_id`로 바뀐다):

```js
// 데모용 전환 가능한 페르소나. 로그인 주체 = 내부 담당자 + VAN 대리점(공용 풀 중 5곳).
// scopeId 는 이제 agency_id 다. (실제로는 Supabase Auth 로그인으로 대체)
export const PERSONAS = [
  { key: "internal", role: "내부",   label: "내부 담당자 (카카오페이)", scopeId: null },
  { key: "AG29",     role: "대리점", label: "대리점: 신신엠엔씨",        scopeId: "AG29" },
  { key: "AG01",     role: "대리점", label: "대리점: (주)그린페이",      scopeId: "AG01" },
  { key: "AG05",     role: "대리점", label: "대리점: (주)다온시스템",    scopeId: "AG05" },
  { key: "AG24",     role: "대리점", label: "대리점: 현페이먼트",        scopeId: "AG24" },
  { key: "AG14",     role: "대리점", label: "대리점: (주)케이에스넷트웍스", scopeId: "AG14" },
];
```

**스코핑을 agency_id 기준으로** (`canSee`·`visibleMerchants`·`visibleClaims`):

```js
// 로그인 주체가 대리점이므로 스코핑 기준은 agency_id 다.
const canSee = useCallback(
  (agencyId) => {
    if (persona.role === "내부") return true;
    return agencyId === persona.scopeId;
  },
  [persona]
);

const visibleMerchants = useMemo(
  () => merchants.filter((m) => canSee(m.agencyId)),
  [merchants, canSee]
);
const visibleClaims = useMemo(
  () => claims.filter((c) => canSee(merchantById(c.merchantId)?.agencyId)),
  [claims, canSee, merchantById]
);
```
> 참고: `incentive_claims.agency_id`가 이미 있어 `c.agencyId`로 바로 스코핑해도 된다.
> 단 접수 시 `merchant.agencyId`를 넣고 있어 둘은 일치한다. 위 코드는 기존 패턴(가맹점에서 파생)을 유지한 형태.

**권한(perms) — "얼라이언스사" → "대리점"**:

```js
canRegister:   persona.role === "대리점",   // was "얼라이언스사"
canSubmitClaim: persona.role === "대리점",   // was "얼라이언스사"
// 내부 권한(승인·검수·정산·되돌리기)은 그대로 persona.role === "내부"
```

**`registerMerchant`** — 대리점은 persona에서, 얼라이언스는 폼 선택값에서:
현재 `registerMerchant`는 `data.agencyId`(폼) + `data.allianceId`(persona)를 쓴다.
호출부(등록 페이지)를 바꿔서 **agencyId=persona.scopeId, allianceId=form.allianceId**가 들어오게 한다. store 함수 시그니처 자체는 그대로 둬도 된다.

### 1-b. `app/merchants/register/page.js` (필드 스왑)

```js
// 로그인한 VAN 대리점 (자동). 얼라이언스는 폼에서 선택한다.
const agency = agencyById(persona.scopeId);
```
- `form`에서 `agencyId` 제거, `allianceId: ""` 추가.
- 제출: `registerMerchant({ ...form, agencyId: persona.scopeId })` (allianceId는 form.allianceId).
- 권한 안내 문구 `얼라이언스사` → `대리점`.
- 필드 두 개 스왑 (store가 `alliances`를 이미 노출함):

```jsx
<Field label="얼라이언스" hint="증빙을 제출할 얼라이언스사를 선택합니다.">
  <Select value={form.allianceId} onChange={set("allianceId")}>
    <option value="">얼라이언스를 선택하세요</option>
    {alliances.map((a) => (
      <option key={a.id} value={a.id}>{a.name}</option>
    ))}
  </Select>
</Field>
<Field label="대리점 (자동)" hint="로그인한 VAN 대리점으로 지정됩니다">
  <Input value={agency?.name || "—"} disabled className="bg-canvas text-ink-500" />
</Field>
```
- `canSave`에 `form.allianceId` 필수 조건 추가(아래 변경 2에서 최종 형태 정리).

### 1-c. `app/incentives/submit/page.js`
- 권한 안내 문구 `얼라이언스사` → `대리점`. 나머지 로직(`merchant.agencyId` 사용)은 그대로 동작한다.
  (승인완료 가맹점 목록이 이제 대리점 스코프로 표시됨.)

### 1-d. `components/Shell.js`
- `roleTone` 분기의 `"얼라이언스사"` → `"대리점"`:
```js
const roleTone =
  persona.role === "내부" ? "bg-kakao-ink text-white"
  : persona.role === "대리점" ? "bg-emerald-500 text-white"
  : "bg-kakao-yellow text-kakao-ink";
```
- 역할 스위처는 `PERSONAS`를 그대로 렌더하므로 내부 + 대리점 5곳이 자동 노출된다(드롭다운/버튼 형태만 확인).

### 1-e. `app/page.js`
- 38행 안내 문구 `얼라이언스↔담당자 흐름` → `대리점↔담당자 흐름` 정도로. `persona.role` 표기는 자동 반영.

---

## 변경 2 — 모집유형을 먼저 선택 + 유형별 입력 분기

**의도**:
- 등록 폼에서 **모집유형을 가장 먼저** 고른다.
- **가맹 계열(가맹선불·가맹후불·PG선불)** = 카카오페이 가맹 유형 → **사업자등록번호 10자리**를 입력하면
  그 사업자번호를 가진 **가맹점/CID가 자동으로 불러와지는** 구조.
- **비가맹 계열(비가맹선불·비가맹후불)** = CID라는 값이 아예 없음 → **사업자번호 + 가맹점명만** 입력.

### 2-a. `lib/mockData.js` — 분류 헬퍼 추가

```js
// 카카오페이 가맹 유형 — 사업자번호↔CID 자동조회 대상 (⚠️ 등록 폼 전용)
// 결제검수용 isGamaeng([가맹선불,가맹후불])과 다르다: 여기엔 PG선불이 포함된다.
export const KAKAOPAY_CID_TYPES = ["가맹선불", "가맹후불", "PG선불"];
export const hasCid = (recruitType) => KAKAOPAY_CID_TYPES.includes(recruitType);
```
> `RECRUIT_TYPES`·`isGamaeng`는 **건드리지 않는다**(결제검수 정책 유지). 아래 "확인 필요" 참고.

### 2-b. `app/merchants/register/page.js` — 단계/입력 재구성

- 진행 단계(STEPS) 첫 라벨 `유형구분` → `모집유형`.
- **① 모집유형 선택**: `RECRUIT_TYPES` 5개를 라디오/버튼 그룹으로. 선택이 바뀌면 아래 조회 상태(cid/name/후보목록)를 초기화한다.
- 가맹점 유형(`STORE_TYPES` 일반가맹점/프랜차이즈)은 그대로 ② 정보 기재 안에 둔다(사용자 요청 밖, 유지).
- **② 정보 기재 (모집유형에 따라 분기)**
  - `hasCid(recruitType) === true` (가맹 계열):
    - **사업자등록번호(10자리)** 입력칸 + 우측에 **[샘플 가맹점 올리기]** 버튼(변경 4).
    - 조회 로직: 사업자번호 10자리가 채워지면 `lookupByBizNo(bizNo)`(변경 4의 샘플 풀) + 기존 `merchants`에서 후보를 찾는다.
      - **1건**: `cid`·`name` 자동 채움.
      - **N건(N:1)**: **CID 선택 드롭다운** 노출 → 고르면 `name` 확정(1:1).
      - **0건**: "샘플 가맹점 올리기로 등록하거나 사업자번호를 확인하세요" 안내(데모라 실제 조회 백엔드 없음).
    - `CID`·`가맹점명`은 조회 결과로 **자동 채움(읽기전용)**. 기존 `inspectCid` 0/O 경고는 유지.
  - `hasCid(recruitType) === false` (비가맹 계열):
    - **사업자등록번호 + 가맹점명 직접 입력**. **CID 필드 없음**(`form.cid = ""` → DB엔 null 저장, store가 이미 `cid || null` 처리).
- **canSave 분기**:
```js
const isCidType = hasCid(form.recruitType);
const canSave =
  form.name && form.bizNo && form.allianceId &&
  (isCidType ? !!form.cid : true);   // 가맹 계열은 CID(자동조회) 필수, 비가맹은 불필요
```

---

## 변경 3 — CID:사업자번호 = N:1, CID:가맹점명 = 1:1

- **스키마 변경 불필요.** `merchants`가 이미 (biz_no, cid, name) 개별 행이라,
  같은 `biz_no`에 `cid`가 다른 여러 행이 존재할 수 있어 N:1이 자연 지원된다.
  (예: 한 사업자번호 아래 치킨집·분식집·무인아이스크림 매장 각각 CID·가맹점명 보유.)
- **UX 반영**: 변경 2의 조회에서 한 사업자번호에 CID가 여러 개면 **CID 선택 단계**를 둔다.
  CID를 고르면 가맹점명이 1:1로 확정된다.
- 샘플 풀(변경 4)도 이 구조(`bizNo → stores[]`)로 만든다.

---

## 변경 4 — 샘플 가맹점 올리기 모달 (테스트 어드민용)

**의도**: 실제 콰트로/내부DB 조회가 아직 없으니, 테스트를 위해 **미등록 사업자번호를 골라 올리는** 버튼을 둔다.
버튼 클릭 → 모달에서 사업자번호(및 그 CID/가맹점명)를 선택하면 폼에 **CID·가맹점명이 자동 입력**된다.

### 4-a. `lib/sampleMerchants.js` — 샘플 참조 풀 (신규 파일)

기존 `merchants.biz_no`와 **충돌하지 않는** 신규 번호로 구성. N:1 예시(한 사업자번호=CID 3개) 포함.

```js
// 테스트 어드민용 샘플 가맹 참조 풀 — 실제 콰트로/내부DB 조회를 대체하는 목데이터.
// 하나의 사업자번호에 CID가 여러 개일 수 있다(N:1). CID↔가맹점명은 1:1.
export const SAMPLE_MERCHANTS = [
  { bizNo: "214-88-01579", stores: [
    { cid: "CQP7H2CHK9N01", name: "행운치킨 역삼점" },        // 치킨집
    { cid: "CQP7H2BNS9N02", name: "김이분식 역삼점" },        // 분식집
    { cid: "CQP7H2ICE9N03", name: "얼음창고 무인아이스크림 역삼점" }, // 무인아이스크림
  ]},
  { bizNo: "305-19-77820", stores: [
    { cid: "CQR3M8CAF1P10", name: "온기카페 성수점" },
  ]},
  { bizNo: "411-30-55214", stores: [
    { cid: "CQP5B1HNW2Q21", name: "청담한우 본점" },
    { cid: "CQP5B1HNW2Q22", name: "청담한우 딜리버리" },
  ]},
  { bizNo: "502-77-31648", stores: [
    { cid: "CQR9P4PIZ7R30", name: "바로피자 마포점" },
  ]},
];

// 사업자번호(하이픈 유무 무관) → stores 목록
export const lookupByBizNo = (bizNo) => {
  const norm = String(bizNo || "").replace(/\D/g, "");
  if (norm.length < 10) return [];
  const hit = SAMPLE_MERCHANTS.find((s) => s.bizNo.replace(/\D/g, "") === norm);
  return hit ? hit.stores : [];
};
```

### 4-b. 버튼 + 모달 (등록 페이지)

- **버튼**: 가맹 계열일 때만 사업자등록번호 우측에 `[샘플 가맹점 올리기]`(ghost 버튼).
- **모달**: `SAMPLE_MERCHANTS`를 사업자번호별로 나열, 각 store(cid,name)를 선택 가능한 행으로.
  - **미등록만 선택 가능**: 기존 `merchants`의 `cid` Set을 만들어, 이미 등록된 CID는 "등록됨" 뱃지 + 비활성 처리.
  - 행 선택 시 `form.bizNo`·`form.cid`·`form.name`을 자동 채우고 모달을 닫는다.
- 모달 구현은 기존 `EditModal`/`RevertMerchantModal`(page.js) 패턴(`fixed inset-0 ... bg-ink-900/40`)을 재사용:

```jsx
function SampleMerchantModal({ registeredCids, onPick, onClose }) {
  return (
    <div className="fixed inset-0 z-40 bg-ink-900/40 flex items-center justify-center p-4" onClick={onClose}>
      <div className="bg-white rounded-2xl shadow-pop w-full max-w-lg" onClick={(e) => e.stopPropagation()}>
        <header className="px-6 py-4 border-b border-line">
          <h3 className="font-bold text-ink-900">샘플 가맹점 올리기</h3>
          <p className="text-xs text-ink-500 mt-1">미등록 사업자번호를 선택하면 CID·가맹점명이 자동 입력됩니다. (테스트용)</p>
        </header>
        <div className="p-6 space-y-4 max-h-[60vh] overflow-y-auto">
          {SAMPLE_MERCHANTS.map((s) => (
            <div key={s.bizNo}>
              <div className="text-xs font-semibold text-ink-700 mb-1.5">{s.bizNo}</div>
              <div className="space-y-1">
                {s.stores.map((st) => {
                  const used = registeredCids.has(st.cid);
                  return (
                    <button
                      key={st.cid}
                      type="button"
                      disabled={used}
                      onClick={() => onPick({ bizNo: s.bizNo, cid: st.cid, name: st.name })}
                      className="w-full flex items-center justify-between rounded-lg border border-line px-3 py-2 text-sm text-left hover:border-kakao-yellowD disabled:opacity-40"
                    >
                      <span>{st.name} <span className="font-mono text-xs text-ink-400">{st.cid}</span></span>
                      {used && <span className="text-[10px] text-ink-400">등록됨</span>}
                    </button>
                  );
                })}
              </div>
            </div>
          ))}
        </div>
        <footer className="px-6 py-4 border-t border-line flex justify-end">
          <Button variant="ghost" onClick={onClose}>닫기</Button>
        </footer>
      </div>
    </div>
  );
}
```
- 등록 페이지에서 `registeredCids = new Set(merchants.map((m) => m.cid).filter(Boolean))`.
  (store에서 `merchants` 노출됨.)

---

## Supabase 변경 (Claude Code가 적용)

**신신엠엔씨 대리점 추가** — `agencies` 컬럼은 `id(text,NN)`·`name(text,NN)`·`bank_account(text,null)`:

```sql
insert into incentive_admin.agencies (id, name, bank_account)
values ('AG29', '신신엠엔씨', null)
on conflict (id) do nothing;
```
- (선택) 신신엠엔씨로 로그인했을 때 목록 데모가 필요하면 `merchants` 1~2건을 `agency_id='AG29'`로 시드.
  비워두고 직접 등록 테스트만 해도 무방하다.
- RLS는 목업 단계 `demo_all`(anon 전체 허용)이라 별도 정책 변경 불필요.

---

## ⚠️ 확인 필요 (정책 — 이번 범위 밖, 추후 확정)

1. **PG선불 분류 이중성**: 이번엔 **등록 단계에서만** PG선불을 가맹(CID 보유) 유형으로 처리한다.
   그러나 결제검수 `isGamaeng(PG선불)=false`라, 시책 '가맹모집' 접수 시 PG선불은 여전히 **'수동판단'**이다.
   즉 등록 시 CID는 채워지지만 결제검수 자동판정 대상엔 안 들어간다.
   → PG선불을 결제검수에서도 '가맹'으로 볼지 정책 확정 필요(맞으면 `isGamaeng`에 PG선불 추가).
2. **기존 시드의 PG선불 2건**(청춘커피·그린샐러드)은 `cid=null`이다. 새 규칙상 CID가 있어야 하지만
   데모 데이터라 그대로 둔다(원하면 backfill).

---

## 🧪 테스트 · 배포 체크리스트

- [ ] `npm run build` 통과 (경고/에러 없음).
- [ ] 역할 스위처: **내부 + 대리점 5곳(신신엠엔씨 포함)** 노출. 대리점 전환 시 등록내역·시책이 **해당 대리점 것만** 보임.
- [ ] 등록 폼: **모집유형 먼저** → 가맹 계열은 사업자번호 조회/샘플버튼으로 **CID·가맹점명 자동**,
      비가맹 계열은 **가맹점명 직접입력·CID 없음**. **얼라이언스 선택 · 대리점 자동**.
- [ ] N:1: 사업자번호 `214-88-01579` 조회 시 CID 3개 중 선택 → 가맹점명 1:1 확정.
- [ ] 샘플 모달: **미등록만 선택 가능**, 선택 시 자동 채움, 등록 후 목록에서 "등록됨" 처리.
- [ ] 비가맹으로 등록한 건은 목록/검수에서 CID `—`로 정상 표시.
- [ ] Supabase에 `AG29 신신엠엔씨` 반영 확인.
- [ ] `git push` → Vercel 자동배포 확인(https://incentive-admin.vercel.app).
- [ ] 완료 후 기존 `HANDOFF.md`의 "역할 스위처에서 VAN대리점 페르소나 제거"·스코핑 설명을 이번 변경에 맞게 갱신.
