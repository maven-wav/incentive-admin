# 🏷 시책 어드민 — 리뷰 수정 핸드오프 R2 (2026-07-24, Claude Code용)

> 라운드 1(대리점 주체 전환 + 모집유형별 등록 분기)은 커밋 `e0ee167`로 배포 완료.
> 이 문서는 **그 위에 올리는 라운드 2 수정 11건**이다. 구현 + 커밋 + 배포(git push → Vercel)는 Claude Code가 진행.
> **claim_type enum 추가(2종) 마이그레이션 포함** + 대리점 AG29(신신엠엔씨)는 라운드 1에서 이미 추가됨.

---

## ⚠️ 전제 (Cowork에서 확정 못 해 기본값으로 진행 — 아니면 뒤집을 것)

**전제 P1 — PG선불 = "가맹"으로 통일한다.**
- **가맹 계열 = `hasCid` = [가맹선불, 가맹후불, PG선불]** (CID 보유 3종).
- **비가맹 계열 = [비가맹선불, 비가맹후불].**
- 이에 맞춰 `lib/mockData.js`의 `isGamaeng`도 **PG선불 포함**으로 통일(아래 8-b). 즉 결제검수도 PG선불을 가맹으로 본다.
- 이 문서 전체가 이 전제로 쓰였다. PG선불을 비가맹으로 두려면: `hasCid`에서 PG선불 유지하되 "승인 스킵/자동 시책"만 `isGamaeng`(가맹선불·가맹후불) 기준으로 바꾸면 된다. (그 경우 8·6·7·H 흐름의 분기 기준을 `isGamaeng`으로 교체)

**전제 P2 (단가)** — 비가맹결제건수 시책 단가는 기존 가맹모집과 동일 **30,000원**으로 둔다(미확정, 임시값).
**전제 P3 (결제건수 입력 주체)** — 비가맹결제건수는 **대리점이 시책 접수 시 입력**한다(담당자는 검수에서 확인·승인).

---

## 분류 헬퍼 정리 (`lib/mockData.js`) — 먼저 통일

```js
// 가맹 계열(카카오페이 가맹 유형, CID 보유) — 등록 시 승인 스킵 + 자동 시책 대상
export const KAKAOPAY_CID_TYPES = ["가맹선불", "가맹후불", "PG선불"];
export const hasCid = (recruitType) => KAKAOPAY_CID_TYPES.includes(recruitType);

// 결제검수도 가맹 = KAKAOPAY_CID_TYPES 로 통일 (전제 P1)
export const isGamaeng = (recruitType) => KAKAOPAY_CID_TYPES.includes(recruitType);
// (기존 GAMAENG_TYPES=[가맹선불,가맹후불] 상수는 제거하거나 isGamaeng을 위와 같이 교체)
```
> `hasCid`와 `isGamaeng`이 이제 동일 집합이 된다. 코드 가독성 위해 하나만 남기고 별칭으로 둬도 된다.

---

# 변경 목록 (11건)

## 1. 샘플 가맹점 올리기 — 버튼/모달 디자인 개선 (`app/merchants/register/page.js`)

현재: 사업자등록번호 옆 ghost 버튼 + 밋밋한 모달. **홈페이지 톤(카카오 옐로우/파트너센터)에 맞춰 다듬는다.**
- **버튼**: 상단 역할 스위처 칩처럼 **칩/뱃지형**으로. 예) 옐로우 아웃라인 + 아이콘(＋) — `사업자등록번호` 라벨 우측 또는 인풋 우측에 배치. 테스트용임이 드러나게 작은 "샘플" 뱃지 유지.
- **모달**: 헤더에 옐로우 포인트, 사업자번호별 카드 그룹핑(라운드/보더/hover), 각 CID 행은 왼쪽 가맹점명·오른쪽 CID(모노) + 상태 뱃지. 미등록/기등록 상태를 색으로 구분(아래 2와 연동). 닫기 외 "직접 입력" 링크 옵션(선택).
- 기능은 그대로. **디자인만** 개선(컴포넌트는 `components/ui.js`의 Card/Button/Pill 톤 재사용).

## 2. 기등록 CID "기등록" 표시 + 등록 차단 (가맹 계열)

사업자번호 입력 후 뜨는 **CID 자동 목록(타이핑 조회 드롭다운 + 샘플 모달)** 양쪽에서 이미 등록된 CID를 **"기등록"으로 표시하고 선택/등록을 막는다.**
- 기등록 판정 집합(아래 3의 "반려 제외"와 동일):
  ```js
  const activeMerchants = merchants.filter((m) => m.status !== MERCHANT_STATUS.REJECTED);
  const registeredCids = new Set(activeMerchants.map((m) => m.cid).filter(Boolean));
  ```
- **CID 선택 드롭다운(candidates)**: 각 옵션에 기등록이면 `(기등록)` 접미사 + `disabled`. 자동 1건 채움 시에도 그 CID가 기등록이면 채우되 **경고 Notice + 저장 차단**.
- **샘플 모달**: 현재 `used` 뱃지("등록됨") 로직을 이 `registeredCids`로 교체(반려 제외 반영), disabled 유지.
- **canSave 보강**: 가맹 계열이고 선택된 `form.cid`가 `registeredCids`에 있으면 저장 불가.
  ```js
  const cidTaken = isCidType && form.cid && registeredCids.has(form.cid);
  const canSave = form.name && form.bizNo && form.allianceId && (isCidType ? !!form.cid : true) && !cidTaken;
  ```
  기등록일 때 `<Notice tone="warn">이미 등록된 CID입니다. (반려·삭제된 건은 재등록 가능)</Notice>` 노출.

## 3. 반려 시 재등록 가능

- 위 `registeredCids`가 **`status !== 반려`만** 집계하므로, **반려된 CID는 자동으로 재등록 허용**된다.
- 별도 로직 불필요 — 2번의 `activeMerchants` 필터가 곧 이 요구를 충족한다.

## 4. 내부 담당자 로우 삭제 (등록내역 + 시책검수)

내부 담당자가 **행 자체를 완전 삭제**하는 기능. DB FK가 전부 `ON DELETE CASCADE`라 종속행(claims·history)은 자동 정리된다.
> ⚠️ **merchants 삭제 시 그 가맹점의 `incentive_claims`도 CASCADE로 함께 삭제된다** — 삭제 확인 모달에 이 사실을 명시.

**`lib/store.js` 액션 추가**:
```js
const deleteMerchant = useCallback(async (id) => {
  const { error } = await supabase.from("merchants").delete().eq("id", id);
  if (error) return fail(error, "가맹점 삭제");
  setMerchants((prev) => prev.filter((m) => m.id !== id));
  setClaims((prev) => prev.filter((c) => c.merchantId !== id)); // CASCADE 반영
  flash("가맹점 삭제 완료 — 연결된 시책도 함께 삭제됨");
}, [flash, fail]);

const deleteClaim = useCallback(async (id) => {
  const { error } = await supabase.from("incentive_claims").delete().eq("id", id);
  if (error) return fail(error, "시책 삭제");
  setClaims((prev) => prev.filter((c) => c.id !== id));
  flash("시책 삭제 완료");
}, [flash, fail]);
```
- `perms`에 `canDelete: persona.role === "내부"` 추가, value에 `deleteMerchant, deleteClaim` 노출.
- **`app/merchants/page.js`**: 내부 담당자일 때 각 행 관리열에 `삭제` 버튼(danger) + 확인 모달("이 가맹점과 연결된 시책까지 삭제됩니다. 계속?").
- **`app/incentives/review/page.js`**: 내부 담당자일 때 각 행 검수열에 `삭제` 버튼 + 확인 모달.
  - **지급확정 건도 개별 삭제 허용**한다(사용자 확정). 단 `지급확정` 행 삭제 시 확인 모달에 **강한 경고**를 노출:
    "지급확정된 시책입니다 — 삭제하면 지급 기록도 사라집니다. 계속하시겠어요?" (버튼 danger).
    즉 되돌리기(승인취소)는 여전히 지급확정 잠금(🔒)이지만, **삭제는 예외적으로 가능**하다. `deleteClaim`은 상태 가드 없이 어떤 상태든 삭제한다.
- ⚠️ **alert/confirm 브라우저 다이얼로그 대신** 기존 코드베이스의 모달 패턴(fixed inset-0)을 사용할 것.

## 5. 삭제 시 재등록 가능
- 삭제로 행이 사라지면 `registeredCids`에서 자동 제외 → **재등록 허용**. 별도 로직 불필요(4번으로 충족).

## 6. 가맹 계열 등록 → 승인 불필요 → 바로 시책검수

**의도**: 가맹이면 등록 시점에 카카오페이가 가맹/활성화/거래건수를 체크하므로 별도 담당자 승인이 필요 없다. 등록 즉시 시책검수로 넘어간다.

**`lib/store.js`의 `registerMerchant` 분기** (가맹 계열이면 승인완료 + 가맹모집 시책 자동 생성):
```js
import { hasCid } from "./mockData";  // 상단 import에 추가

const registerMerchant = useCallback(async (data) => {
  const gamaeng = hasCid(data.recruitType);
  const { data: row, error } = await supabase
    .from("merchants")
    .insert({
      id: nextIdFrom(merchants, "M"),
      name: data.name, biz_no: data.bizNo, cid: data.cid || null,
      store_type: data.storeType, prepaid_type: data.recruitType,
      agency_id: data.agencyId, alliance_id: data.allianceId,
      // 가맹은 승인 스킵 → 바로 승인완료. 비가맹은 기존대로 등록대기.
      status: gamaeng ? MERCHANT_STATUS.APPROVED : MERCHANT_STATUS.PENDING,
      registered_at: today(),
    })
    .select().single();
  if (error) return fail(error, "가맹점 등록");
  const m = toMerchant(row);
  setMerchants((prev) => [m, ...prev]);

  if (gamaeng) {
    // 가맹모집 시책 자동 생성 → 시책검수(검수대기)로 바로 진입
    const { data: crow, error: cerr } = await supabase
      .from("incentive_claims")
      .insert({
        id: nextIdFrom(claims, "C"),
        merchant_id: m.id, agency_id: m.agencyId,
        type: CLAIM_TYPES.RECRUIT, claim_month: today().slice(0, 7),
        status: CLAIM_STATUS.REVIEW,
        is_active: false, txn_total: 0,   // 실연동 시 콰트로가 채움
        proof_photo: null, amount: INCENTIVE_AMOUNT[CLAIM_TYPES.RECRUIT],
      })
      .select().single();
    if (cerr) { fail(cerr, "가맹 시책 자동생성"); }
    else {
      const created = toClaim(crow);
      setClaims((prev) => [created, ...prev]);
      await logStatus([crow], null, persona.label, "가맹 등록 자동 접수");
    }
    flash(`가맹점 '${m.name}' 등록 완료 — 시책검수 자동 접수됨`);
  } else {
    flash(`가맹점 '${m.name}' 등록 완료 — 담당자 승인 대기`);
  }
  return { ...m, gamaeng };
}, [merchants, claims, flash, fail, logStatus, persona]);
```
- **등록 페이지 이동**: 가맹·비가맹 **둘 다 `/merchants`(등록내역)로** 이동한다.
  ```js
  const created = await registerMerchant({ ...form, agencyId: persona.scopeId });
  if (created) router.push("/merchants");
  ```
  > ⚠️ **`/incentives/review`(시책검수)로 보내지 말 것.** 로그인 주체가 대리점인데 시책검수는 **내부 담당자 전용** 화면이라
  > "내부 담당자 전용입니다" 경고만 뜬다(테스트에서 확인됨). 가맹 시책은 백그라운드로 시책검수에 자동 접수되고,
  > 대리점은 등록내역에서 `승인완료`로 뜬 자기 등록을 확인하면 된다. 자동 접수 사실은 위 flash 토스트로 알린다.
- 등록내역에서 가맹 건은 `승인완료`로 뜬다(등록대기 큐에 안 쌓임).

## 7. 비가맹 등록 승인 절차 유지
- 비가맹선불·비가맹후불은 **기존대로** 등록대기 → 담당자 승인 → 승인완료. (6번 분기에서 자동 처리됨)

## 8. 시책 접수 유형 3가지로 재정의 (`app/incentives/submit/page.js` + store + enum)

**시책 접수에서 고를 수 있는 유형 = 3가지. 가맹모집(가맹)은 접수에서 제외**(6번에서 자동 생성됨).

| 유형 | 대상 가맹점 | 입력 | 단가 |
|---|---|---|---|
| **비가맹결제건수** | 비가맹 & 승인완료 | 결제건수(숫자), 활성화 체크 안 함 | 30,000 (P2) |
| **얼라이언스QR부착** | 승인완료 전체 | 부착 사진(20개 이상) | 20,000 |
| **홍보물부착** | 승인완료 전체 | 부착 사진 | 20,000 |

### 8-a. `lib/mockData.js`
```js
export const CLAIM_TYPES = {
  RECRUIT: "가맹모집",          // 가맹 등록 시 자동생성 (접수 화면에는 없음)
  NONGAMAENG_TXN: "비가맹결제건수",
  ALLIANCE_QR: "얼라이언스QR부착",
  PROMO: "홍보물부착",
};
export const INCENTIVE_AMOUNT = {
  [CLAIM_TYPES.RECRUIT]: 30000,
  [CLAIM_TYPES.NONGAMAENG_TXN]: 30000,   // 임시(P2)
  [CLAIM_TYPES.ALLIANCE_QR]: 20000,
  [CLAIM_TYPES.PROMO]: 20000,
};
// 시책 접수에서 노출할 유형(가맹모집 제외)
export const SUBMITTABLE_CLAIM_TYPES = [
  CLAIM_TYPES.NONGAMAENG_TXN, CLAIM_TYPES.ALLIANCE_QR, CLAIM_TYPES.PROMO,
];
export const PHOTO_CLAIM_TYPES = [CLAIM_TYPES.ALLIANCE_QR, CLAIM_TYPES.PROMO]; // 사진 필요
```

### 8-b. DB enum 마이그레이션 (Claude Code가 apply_migration)
```sql
ALTER TYPE incentive_admin.claim_type ADD VALUE IF NOT EXISTS '비가맹결제건수';
ALTER TYPE incentive_admin.claim_type ADD VALUE IF NOT EXISTS '얼라이언스QR부착';
```
> claim_type 현재 값: 가맹모집·홍보물부착. 위 2종 추가. (기존 '가맹모집'은 자동 시책용으로 유지)

### 8-c. `app/incentives/submit/page.js`
- 유형 카드 3개(`SUBMITTABLE_CLAIM_TYPES`)로 교체. `가맹모집` 카드 제거.
- **대상 가맹점 필터**:
  ```js
  const approved = visibleMerchants.filter((m) => m.status === MERCHANT_STATUS.APPROVED);
  const targets = type === CLAIM_TYPES.NONGAMAENG_TXN
    ? approved.filter((m) => !hasCid(m.recruitType))   // 비가맹만
    : approved;                                          // QR·홍보물은 전체
  ```
- **비가맹결제건수** 선택 시: 결제건수 숫자 입력 필드 노출(활성화 입력 없음). `canSubmit`에 `txnTotal>0` 조건.
- **사진 필요**: `const needPhoto = PHOTO_CLAIM_TYPES.includes(type);` (QR·홍보물 둘 다). QR은 hint "얼라이언스 QR 20개 이상 부착 사진".
- **금액 안내**: `INCENTIVE_AMOUNT[type]`.

### 8-d. `lib/store.js` `submitClaim` 확장 (txnTotal 수용 + 유형별 필드)
```js
const submitClaim = useCallback(async ({ merchantId, agencyId, type, claimMonth, proofPhoto, txnTotal }) => {
  const dup = claims.find((c) => c.merchantId === merchantId && c.type === type && c.status !== CLAIM_STATUS.REJECTED);
  if (dup) { flash("이미 접수된 동일 유형 시책이 있습니다."); return null; }

  const needPhoto = PHOTO_CLAIM_TYPES.includes(type);
  const isTxn = type === CLAIM_TYPES.NONGAMAENG_TXN;

  const { data: row, error } = await supabase.from("incentive_claims").insert({
    id: nextIdFrom(claims, "C"),
    merchant_id: merchantId, agency_id: agencyId, type, claim_month: claimMonth,
    status: CLAIM_STATUS.REVIEW,
    is_active: null,                                  // 비가맹은 활성화 체크 안 함
    txn_total: isTxn ? (txnTotal ?? 0) : null,        // 비가맹결제건수만 값 저장
    proof_photo: needPhoto ? (proofPhoto || "부착사진_증빙.jpg") : null,
    amount: INCENTIVE_AMOUNT[type],
  }).select().single();
  if (error) return fail(error, "시책 접수");
  const created = toClaim(row);
  setClaims((prev) => [created, ...prev]);
  await logStatus([row], null, persona.label, "증빙 접수");
  flash("시책 접수 완료 — 검수 대기");
  return created;
}, [claims, flash, fail, logStatus, persona]);
```

### 8-e. `app/incentives/review/page.js` (검수 화면 반영)
- `결제검수` 열: 유형별로
  - `비가맹결제건수` → 결제건수 `{c.txnTotal}건` + "수동판단"(활성화 표기 없음).
  - `얼라이언스QR부착`/`홍보물부착` → 🖼 사진 보기.
  - `가맹모집`(자동생성) → 기존 `PaymentReview`(활성화·건수·자동충족).
- `TypeTag` 색상 맵(`components/ui.js`)에 신규 2종 추가:
  ```js
  비가맹결제건수: "bg-violet-50 text-violet-700 border-violet-100",
  얼라이언스QR부착: "bg-teal-50 text-teal-700 border-teal-100",
  ```

## 9. 동일 사업자번호 기등록 표시 (등록 시)
- 등록 폼에서 `form.bizNo`(10자리)로 **기등록 가맹점(반려 제외)**을 찾아 안내:
  ```js
  const sameBiz = activeMerchants.filter((m) => m.bizNo && m.bizNo.replace(/\D/g,"") === bizDigits);
  ```
- `sameBiz.length > 0`이면 정보 카드/Notice로 "이 사업자번호로 등록된 가맹점 N곳" + 목록(가맹점명·CID·상태) 노출.

## 10. 비가맹 동일 사업자번호 — 알림하되 막지 않음
- **비가맹 계열**에서 `sameBiz.length > 0`이면 **모달/얼럿(모달 권장)** 으로 "같은 사업자번호로 등록된 가맹점이 있습니다"를 보여주되 **저장은 막지 않는다**(한 사업자번호로 여러 가게 운영 가능).
  - 저장 버튼 클릭 시 sameBiz가 있으면 확인 모달 → "그래도 등록" 진행. (가맹 계열은 9번 안내 + CID 단위 차단(2번)으로 충분하니 이 모달은 비가맹에만.)

## 11. 검수 시 기등록 경고 + 클릭 시 목록
- **등록 검수(비가맹 승인) — `app/merchants/page.js`**: 각 행에서 같은 사업자번호로 등록된 다른 가맹점이 있으면(반려 제외, 자기 자신 제외) **⚠️ 경고 뱃지 "같은 사업자로 등록된 가맹점이 있어요"**. 클릭하면 모달로 해당 사업자번호의 가맹점 목록(가맹점명·CID·모집유형·대리점·상태) 표시.
  ```js
  const siblings = (m) => merchants.filter((x) => x.id !== m.id && x.status !== MERCHANT_STATUS.REJECTED
    && x.bizNo && x.bizNo.replace(/\D/g,"") === m.bizNo.replace(/\D/g,""));
  ```
- (선택) 시책검수 화면에도 동일 뱃지 적용 가능. 우선순위는 등록내역.

## 12. 대리점 '시책 내역' 페이지 (내가 접수한 시책 조회 + 잘못 올린 건 자가 철회)

**문제**: 시책 접수 후 `/incentives/review`(시책검수)로 보내는데 그 화면은 **내부 담당자 전용**이라 대리점은 경고만 본다(6번과 동일 이슈). 대리점이 **자기가 접수한 시책을 확인하고, 잘못 올린 건은 스스로 철회**할 화면이 필요하다.

- **신규 페이지 `app/incentives/mine/page.js`** — 라벨 "시책 내역".
  - 대리점(및 내부) 접근. `visibleClaims`(대리점 스코프라 자기 시책만) 표시.
  - 컬럼: 가맹점 · 시책유형 · 모집유형 · 접수월 · 증빙/결제검수 · 금액 · 상태.
  - **검수대기 건에 한해 대리점이 직접 철회(삭제)** — 확인 모달(fixed inset-0 패턴). 정산대기·지급확정·반려는 철회 불가(담당자 검토 이후라 이력 보존; 반려 건은 다시 접수 가능하니 별도 삭제 불필요).
- **`components/Shell.js` NAV에 추가**:
  ```js
  { href: "/incentives/mine", label: "시책 내역", icon: "≣", show: (p) => p.canSubmitClaim },
  ```
- **`lib/store.js` — 대리점 자가 철회 액션**(내부 담당자용 `deleteClaim`과 분리):
  ```js
  const withdrawClaim = useCallback(async (id) => {
    const c = claims.find((x) => x.id === id);
    if (!c || c.status !== CLAIM_STATUS.REVIEW) { flash("검수대기 상태에서만 철회할 수 있습니다."); return; }
    const { error } = await supabase.from("incentive_claims")
      .delete().eq("id", id).eq("status", CLAIM_STATUS.REVIEW);   // 경합 방지: 검수대기일 때만
    if (error) return fail(error, "시책 철회");
    setClaims((prev) => prev.filter((x) => x.id !== id));
    flash("시책 철회 완료 — 접수가 취소되었습니다");
  }, [claims, flash, fail]);
  ```
  - `perms`에 `canWithdrawOwnClaim: persona.role === "대리점"` 추가, value에 `withdrawClaim` 노출.
- **시책 접수 후 이동**: `app/incentives/submit/page.js`의 성공 후 `router.push("/incentives/review")` → **`router.push("/incentives/mine")`** 로 변경.
- 참고: 가맹 등록 시 자동 생성되는 가맹모집 시책도 이 목록에 뜬다. 검수대기면 대리점이 철회 가능(원치 않으면 자동 가맹모집은 철회 대상에서 제외 — `type !== 가맹모집` 조건 추가).

## 13. 권한-이동 정합성 — 다른 역할 전용 페이지로 보내는 곳 전수 수정

**원칙**: 역할을 분리했으니 **어떤 액션 후에도 현재 로그인 주체가 접근 가능한 페이지로만 이동**한다. 내부 전용(`/incentives/review`)·대리점 전용(`/incentives/submit`, `/incentives/mine`, `/merchants/register`) 페이지를 상대 역할에게 링크/리다이렉트하면 "전용 화면입니다" 경고만 뜬다. 코드 전수 조사 결과 **위반 4곳**(전부 대리점 → 내부 전용 `/incentives/review`로 보냄):

| # | 위치 | 현재 이동 | 주체 | 수정 |
|---|---|---|---|---|
| a | `app/incentives/submit/page.js:119` (접수 성공) | `/incentives/review` | 대리점 | **`/incentives/mine`** |
| b | `app/incentives/submit/page.js:249` (취소 버튼) | `/incentives/review` | 대리점 | **`/incentives/mine`** (또는 `/merchants`) |
| c | `app/merchants/register/page.js:178` (가맹 등록 성공) | `/incentives/review` | 대리점 | **`/merchants`** (6번과 동일) |
| d | `app/page.js:91` (대시보드 "시책 검수로 이동" 링크) | `/incentives/review` | 모든 역할(대리점 포함) | **역할 분기**: 내부→`/incentives/review`, 대리점→`/incentives/mine` |

- **d 상세**: 대시보드는 전 역할 공용이라 이 링크가 대리점에게도 보인다. 링크 target과 라벨을 역할로 분기:
  ```js
  const { persona } = useStore();
  const isStaff = persona.role === "내부";
  // ...
  <Link href={isStaff ? "/incentives/review" : "/incentives/mine"} ...>
    {isStaff ? "시책 검수로 이동 →" : "내 시책 내역 보기 →"}
  </Link>
  ```
  (선택) 같은 카드의 제목/설명 "검수 대기 시책 · 콰트로/사진 검수"도 대리점에겐 "내 검수대기 시책"으로 문구 분기하면 더 자연스럽다.

**문제 없음(확인함)**: `Shell.js` 네비게이션은 전부 `show:(p)=>p.canX`로 역할 게이팅됨 / `/settlements`·`/merchants`·`/`(대시보드)는 전 역할 공용 조회 화면이라 OK / **내부를 대리점 전용 페이지로 보내는 경로는 없음**.

---

## 🗄 DB 변경 요약 (Claude Code가 apply)
1. `claim_type` enum에 `비가맹결제건수`, `얼라이언스QR부착` 추가 (8-b).
2. 컬럼 추가 없음 — 삭제는 기존 FK CASCADE로 처리, 결제건수는 기존 `txn_total` 재사용.
3. RLS `demo_all` 유지(anon 전체 허용)라 delete도 통과. 실서비스 전 역할별 정책으로 교체 필요.

## ✅ 확인 필요 (정책 — 추후 확정)
- **P1 PG선불 분류**(가맹 통일) — 이 문서 전제. 아니면 6·7·8 분기 기준을 `isGamaeng`(가맹선불·가맹후불)로 교체.
- **P2 비가맹결제건수 단가** 30,000 임시.
- **P3 비가맹결제건수 입력 주체** = 대리점(접수). 담당자 검수 입력으로 바꿀 수도 있음.
- 얼라이언스QR "20개 이상" — 현재 사진 + 안내문구만. 개수 입력/검증이 필요하면 `txn_total` 또는 신규 컬럼로 저장.

## 🧪 테스트 · 배포 체크리스트
- [ ] `npm run build` 통과.
- [ ] 가맹 계열(가맹선불/가맹후불/PG선불) 등록 → 승인 단계 없이 **시책검수에 가맹모집 자동 등장**, 등록내역엔 승인완료. **등록 직후 화면은 등록내역**(시책검수 아님 — 대리점은 접근 불가).
- [ ] 내부 담당자로 전환 시 그 가맹 시책이 시책검수 목록에 보임.
- [ ] **대리점으로 어떤 액션을 해도 '내부 담당자 전용' 경고 화면에 안 떨어짐** (접수 성공/취소, 가맹 등록, 대시보드 링크 전부 확인 — 13번).
- [ ] 대리점 '시책 내역'(/incentives/mine)에서 내 시책 조회 + 검수대기 건 철회 동작.
- [ ] 지급확정 시책도 내부 담당자가 개별 삭제 가능(강한 경고 모달). 되돌리기(승인취소)는 여전히 잠금.
- [ ] 비가맹 계열 등록 → 등록대기 → 담당자 승인 → 승인완료(기존 흐름).
- [ ] 기등록 CID: 사업자번호 조회/샘플 모달에서 **기등록 표시 + 선택·저장 차단**. **반려/삭제 건은 재등록 허용**.
- [ ] 내부 담당자: 등록내역·시책검수에서 **행 삭제**(모달 확인). 가맹점 삭제 시 연결 시책도 사라짐. 삭제 후 재등록 가능.
- [ ] 시책 접수 3유형만 노출(가맹모집 없음). 비가맹결제건수=결제건수 입력, QR·홍보물=사진+2만원.
- [ ] 동일 사업자번호: 등록 시 기등록 목록 노출. 비가맹은 모달로 알리되 저장 허용.
- [ ] 등록내역 검수 행에 기등록 ⚠️ 뱃지 → 클릭 시 같은 사업자 목록 모달.
- [ ] 샘플 버튼/모달 디자인이 홈 톤과 어울림.
- [ ] `git push` → Vercel 자동배포 확인.
- [ ] 완료 후 `HANDOFF.md` 갱신(시책 유형 3종, 가맹 자동시책, 삭제/재등록 정책).
